{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@dark-web-alert-detection/auth": "workspace:*",
    "@dark-web-alert-detection/db": "workspace:*",
    "@dark-web-alert-detection/env": "workspace:*",
    "@elysiajs/cors": "^1.4.1",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "elysia": "^1.4.21",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@dark-web-alert-detection/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "^5"
  }
}
