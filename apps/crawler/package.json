{
  "name": "crawler",
  "type": "module",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "crawl": "bun run src/index.ts",
    "crawl:watch": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "check-types": "tsc -b",
    "seed": "bun run src/seed.ts"
  },
  "dependencies": {
    "@dark-web-alert-detection/db": "workspace:*",
    "@dark-web-alert-detection/detection": "workspace:*",
    "@dark-web-alert-detection/env": "workspace:*",
    "cheerio": "^1.0.0",
    "dotenv": "catalog:",
    "socks": "^2.8.7",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@dark-web-alert-detection/config": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "^5"
  }
}
